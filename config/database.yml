default: &default
  adapter: postgresql
  encoding: utf8
  pool: 5
  username: root
  password:
  host: localhost
development:
  <<: *default
  database: chatbox_development
test:
  <<: *default
  database: chatbox_test
